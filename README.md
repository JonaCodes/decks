## Template guidelines

- This includes a bunch of stuff from routes, auth, linting, llm stuff, DB,
  models, etc.
- Run `pnpm i` in root, public, and server
- Run `psql -d postgres -c "CREATE DATABASE PROJECT_NAME_dev;`
- change every instance of PROJECT_NAME to your project name
