{
  "name": "PROJECT_NAME",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --dir public run dev & pnpm --dir server run dev & wait",
    "build": "cd public && pnpm install --frozen-lockfile && pnpm run build && cd .. && cd server && pnpm install --frozen-lockfile && pnpm run build",
    "start": "node server/dist/server.cjs",
    "migrate": "cd server && npx sequelize-cli db:migrate --config config/config.cjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "dotenv": "^17.3.1",
    "globals": "^17.4.0",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.4",
    "sequelize-cli": "^6.6.5",
    "tsx": "^4.21.0"
  },
  "engines": {
    "node": "20.x"
  },
  "overrides": {
    "rollup": "npm:@rollup/wasm-node@^4.40.2"
  },
  "dependencies": {
    "react-markdown": "^10.1.0"
  }
}
